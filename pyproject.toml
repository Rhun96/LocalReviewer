[project]
name = "localreviewer"
version = "0.1.0"
description = "Локальное десктоп-приложение для ревью и разметки датасетов"
requires-python = ">=3.11"
dependencies = [
  "PySide6==6.7.2",
  "openpyxl==3.1.5",
  "matplotlib==3.8.4",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "W", "B", "UP"]
